Umlg is a uml to java code generation tool. From uml class diagrams it generates a persistent entity domain model. The entities are persisted in a graph database. For this tinkerpop blueprints is used.
